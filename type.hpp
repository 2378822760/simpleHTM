#ifndef TYPE_HPP
#define TYPE_HPP

#endif // TYPE_HPP

/* SpatialPooler Using */
typedef unsigned int UInt;
typedef float Real;

/* TemporalMemory Using */
typedef UInt ColumnIndex;
typedef UInt CellIndex;
typedef UInt SegIndex;
typedef float Permanence;
